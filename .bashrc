alias la='ls -al'
alias ll='ls -ll'

alias ..='cd ..'
alias ...='../..'
alias ....='../../..'
alias -- -='cd -'

alias e='explorer'

# git aliases
alias gst='git status'
alias gai='git add -i'
alias gap='git add -p'
alias gcm='git commit -m'

alias mvn=mvn.sh

[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm"
